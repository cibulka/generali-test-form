import styled from '@emotion/styled';

export const StyledLangNav = styled.nav`
  ul {
    display: flex;
    justify-content: flex-end;
    align-items: center;
    gap: 1em;
  }
  a {
    text-decoration: underline;
  }
  strong {
    font-weight: bold;
  }
`;
